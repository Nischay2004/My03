---
 - name: Deploy Tomcat
   hosts: web
   become: yes
   tasks:
    - name: Copy WAR file
      copy:
        src: "/var/snap/jenkins/"
        dest: "/opt/tomcat/webapps/My03.war"
        remote_src: no
        
    - name: Restart Tomcat
      systemd:
       - name: tomcat
         state: restarted
